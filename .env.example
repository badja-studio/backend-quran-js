# Development Environment
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=quran_db
DB_USER=quran_user
DB_PASSWORD=quran_password

# API Documentation
API_TITLE=Quran API
API_VERSION=1.0.0
API_DESCRIPTION=API for Quran management system

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_ACCESS_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=30m

# CORS Configuration
# Comma-separated list of allowed origins
# Use '*' to allow all origins (not recommended for production)
# Example: CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com
CORS_ALLOWED_ORIGINS=*

# Rate Limiting Configuration
# General rate limiter (applies to all requests except health check)
RATE_LIMIT_WINDOW_MS=900000          # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100          # Max requests per window per IP

# Authentication rate limiter (more restrictive for login/auth endpoints)
AUTH_RATE_LIMIT_WINDOW_MS=900000     # 15 minutes in milliseconds
AUTH_RATE_LIMIT_MAX_REQUESTS=5       # Max auth attempts per window per IP (production)

# API rate limiter (for protected API endpoints)
API_RATE_LIMIT_WINDOW_MS=900000      # 15 minutes in milliseconds  
API_RATE_LIMIT_MAX_REQUESTS=200      # Max API requests per window per IP

# Master data rate limiter (more permissive for dropdown data)
MASTER_DATA_RATE_LIMIT_WINDOW_MS=900000  # 15 minutes in milliseconds
MASTER_DATA_RATE_LIMIT_MAX_REQUESTS=300  # Max master data requests per window per IP

# Development vs Production Rate Limiting Guidelines:
# Development: More permissive limits for easier testing
# Production: Stricter limits for DDoS protection
# 
# Development suggested values:
# - RATE_LIMIT_MAX_REQUESTS=300
# - AUTH_RATE_LIMIT_MAX_REQUESTS=20
# - API_RATE_LIMIT_MAX_REQUESTS=500
# - MASTER_DATA_RATE_LIMIT_MAX_REQUESTS=1000
#
# Production suggested values:
# - RATE_LIMIT_MAX_REQUESTS=100
# - AUTH_RATE_LIMIT_MAX_REQUESTS=5
# - API_RATE_LIMIT_MAX_REQUESTS=200
# - MASTER_DATA_RATE_LIMIT_MAX_REQUESTS=300
